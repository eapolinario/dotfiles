# Compatibility placeholder for existing Stow links. Global tmpfiles cleanup
# must never empty Downloads. Opt in through omarchy/install.sh; its private
# rule lives outside user-tmpfiles.d and is read only by the dedicated service.
